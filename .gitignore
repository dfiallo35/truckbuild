# Environment
.env
.env.local
.env*.local

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# CodeGraph — the symbol index is built from the source on each machine,
# so it is local state, not shared state. Never commit the database.
.codegraph/

# OS / editor
.DS_Store
