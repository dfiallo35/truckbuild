.next/
node_modules/
pnpm-lock.yaml

# Playwright run artifacts. Gitignored, but Prettier walks the working tree rather than the
# index, so `--check` fails on them in CI after a local test run.
playwright-report/
test-results/
blob-report/
